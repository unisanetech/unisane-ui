name: Publish UI prerelease

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-ui-prerelease
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Require the canonical main branch
        run: test "$GITHUB_REF" = refs/heads/main

      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.26.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          registry-url: https://registry.npmjs.org/
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Require the compatible canonical CLI host
        run: |
          test "$(npm view unisane@0.1.0 version)" = "0.1.0"
          npm view unisane@0.1.0 repository.url | grep -F "github.com/unisanetech/unisane-ops"

      - name: Verify public release boundary
        run: |
          pnpm check:boundaries
          pnpm check:release-readiness
          pnpm --filter @unisane/ui-cli check-types
          pnpm --filter @unisane/ui-cli test
          pnpm check:package-content
          pnpm check:packed-producer-certificate
          pnpm check:email-templates-packed-producer-certificate
          git diff --exit-code

      - name: Publish tokens prerelease
        working-directory: packages/tokens
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --tag next --provenance

      - name: Publish UI prerelease
        working-directory: packages/ui
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --tag next --provenance

      - name: Publish UI registry pack prerelease
        working-directory: packages/ui-cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --tag next --provenance

      - name: Publish DataTable prerelease
        working-directory: packages/data-table
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --tag next --provenance

      - name: Publish email templates prerelease
        working-directory: packages/email-templates
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --tag next --provenance
